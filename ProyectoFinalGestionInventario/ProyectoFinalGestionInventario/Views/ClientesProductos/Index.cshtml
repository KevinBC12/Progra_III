@model IEnumerable<ProyectoFinalGestionInventario.Models.ClienteProducto>

@{
    ViewData["Title"] = "Inventario";
}

<div class="container-fluid py-5" style="background-image: url('/image/ImagenInventario.jpg'); background-size: cover; background-position: center; min-height: 100vh;">
    <div class="bg-light rounded p-4 shadow">
        <h1 class="mb-4">Inventario</h1>
        <p>
            <a class="btn btn-primary" asp-action="Create">Agregar nuevo</a>
        </p>
        <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Producto.Nombre)</th>
                    <th>@Html.DisplayNameFor(model => model.Cliente.Nombre)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Producto.Nombre)</td>
                        <td>@Html.DisplayFor(modelItem => item.Cliente.Nombre)</td>
                        <td>
                            <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                            <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@item.Id">Detalles</a>
                            <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
